# Placeholder — actual values are injected by Terraform (kubernetes_secret_v1)
# or an External Secrets Operator SecretStore. Do NOT commit real tokens here.
#
# Required keys:
#   slack-token      — bot OAuth token (xoxb-...)
#   email-username   — SMTP username / sender address
#   email-password   — SMTP password (if applicable)
apiVersion: v1
kind: Secret
metadata:
  name: argocd-notifications-secret
  namespace: argocd
type: Opaque
stringData: {}
